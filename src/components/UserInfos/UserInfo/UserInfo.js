import styles from './UserInfo.module.css';
import React from 'react';
const UserInfo =(name)=>{
    return (
        <div className='styles.infoContainer'>{name}</div>
    )
}
export default UserInfo;